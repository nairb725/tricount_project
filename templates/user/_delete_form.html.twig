<form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-primary mt-3">Delete</button>
      <a class="btn btn-primary mt-3" href="{{ path('user_index') }}">back to list</a>
    <a class="btn btn-primary mt-3" href="{{ path('user_edit', {'id': user.id}) }}">edit</a>
</form>
