{% extends 'base.html.twig' %}

{% block title %}Expense{% endblock %}

{% block body %}
    <h1 class="text-center mt-3 mb-3">Expense</h1>
    <body class="container"> 
<div class="row">
<div class="col-3"></div>
<div class="col-6">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ expense.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ expense.price }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('expense_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('expense_edit', {'id': expense.id}) }}">edit</a>

    {{ include('expense/_delete_form.html.twig') }}
    </div>  
<div class="col-3"></div>  
</body>
{% endblock %}
