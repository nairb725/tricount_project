{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn btn-primary mt-2" href="{{ path('expense_index') }}">Save </button>
<a class="btn btn-primary mt-2" href="{{ path('expense_index') }}">back to list</a>
{{ form_end(form) }}
