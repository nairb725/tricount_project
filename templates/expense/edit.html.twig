{% extends 'base.html.twig' %}

{% block title %}Edit Expense{% endblock %}

{% block body %}
<body class="container"> 
<div class="row">
<div class="col-4"></div>
<div class="col-4">
    <h1>Edit Expense</h1>

   <a  {{ include('expense/_form.html.twig', {'button_label': 'Update'}) }}</a>

    <a {{ include('expense/_delete_form.html.twig') }} </a>
    <a class="btn btn-primary mt-3" href="{{ path('expense_index') }}">back to list</a>
    <div class="col-4"></div>  
</body>
{% endblock %}
