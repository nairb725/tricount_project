<form method="post" action="{{ path('expense_delete', {'id': expense.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expense.id) }}">
    <button class="btn btn-primary mt-3">Delete</button>
    <a class="btn btn-primary mt-3" href="{{ path('expense_index') }}">back to list</a>
    <a class="btn btn-primary mt-3" href="{{ path('expense_edit', {'id': expense.id}) }}">edit</a>
</form>
