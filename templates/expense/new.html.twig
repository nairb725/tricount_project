{% extends 'base.html.twig' %}

{% block title %}New Expense{% endblock %}

{% block body %}
<div class="container">
<div class="row">
<div class="col-4"></div>
<div class="col-4">
    <h1>Create new Expense</h1>

   <a {{ include('expense/_form.html.twig') }} </a>

      <a class="btn btn-primary mt-3" href="{{ path('expense_index') }}">back to list</a>
    </div>  
<div class="col-4"></div>  
{% endblock %}
