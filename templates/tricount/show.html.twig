{% extends 'base.html.twig' %}

{% block title %}Tricount{% endblock %}

{% block body %}
    <h1>Tricount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tricount.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ tricount.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ tricount.description }}</td>
            </tr>
            <tr>
                <th>Currency</th>
                <td>{{ tricount.currency }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tricount_index') }}">back to list</a>

    <a href="{{ path('tricount_edit', {'id': tricount.id}) }}">edit</a>

    {{ include('tricount/_delete_form.html.twig') }}
{% endblock %}
