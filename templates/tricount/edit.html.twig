{% extends 'base.html.twig' %}

{% block title %}Edit Tricount{% endblock %}

{% block body %}
<body class="container"> 
<div class="row">
<div class="col-4"></div>
<div class="col-4">
    <h1>Edit Tricount</h1>

   <a {{ include('tricount/_form.html.twig', {'button_label': 'Update'}) }}</a>

    <a class="btn btn-primary mt-2" href="{{ path('tricount_index') }}">back to list</a>

  <a {{ include('tricount/_delete_form.html.twig') }}</a>
    <div class="col-4"></div>  
</body>
{% endblock %}
