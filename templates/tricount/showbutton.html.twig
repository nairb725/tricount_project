<form method="post" action="{{ path('tricount_delete', {'id': tricount.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tricount.id) }}">
    <button class="btn btn-primary mt-2">Delete</button>
    <a class="btn btn-primary mt-2" href="{{ path('tricount_index') }}">back to list</a>
    <a class="btn btn-primary mt-2" href="{{ path('tricount_edit', {'id': tricount.id}) }}">edit</a>
</form>
       